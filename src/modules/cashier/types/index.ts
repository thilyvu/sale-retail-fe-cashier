export enum OrderStatus {
	PENDING = "pending",
	CONFIRMED = "confirmed",
	CANCELLED = "cancelled",
	COMPLETED = "completed",
}

export enum PaymentMethod {
	CASH = "cash",
	TRANSFER = "transfer",
	CREDIT = "credit",
}

export enum DiscountType {
	PERCENTAGE = "percentage",
	AMOUNT = "amount",
}
